using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GetKey : MonoBehaviour
{
    private void OnTriggerStay(Collider other)
    {
        /*
        UIManager._instance.keyPanel.SetActive(true);
        if (Input.GetKeyDown(KeyCode.E) && other.gameObject.CompareTag("Player"))
        {
            GameManager._instance.gotKey = true;
            UIManager._instance.keyPanel.SetActive(false);
            Destroy(gameObject);
            GameManager._instance.firstTrigger();

        }
        */
    }

    
}
